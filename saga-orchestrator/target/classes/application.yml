server:
  port: 8086

spring:
  application:
    name: saga-orchestrator

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.shopeasy.saga: DEBUG
